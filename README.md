# Warehouse-Management-System
A C-based warehouse management system that generates and processes container data, sorts deliveries chronologically using timestamps, and merges individual containers into a single dispatch list. It also implements binary search for time based queries and frequency counting the amount of products within each container.
